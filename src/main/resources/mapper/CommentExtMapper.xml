<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="life.majiang.community.community.mapper.CommentExtMapper">
    <resultMap id="BaseResultMap" type="life.majiang.community.community.model.Comment">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Sat Nov 28 14:41:26 CST 2020.
        -->
        <id column="ID" jdbcType="BIGINT" property="id" />
        <result column="PARENT_ID" jdbcType="BIGINT" property="parentId" />
        <result column="TYPE" jdbcType="INTEGER" property="type" />
        <result column="COMMENTATOR" jdbcType="BIGINT" property="commentator" />
        <result column="CREATETIME" jdbcType="BIGINT" property="createtime" />
        <result column="MODIFYTIME" jdbcType="BIGINT" property="modifytime" />
        <result column="LIKE_COUNT" jdbcType="BIGINT" property="likeCount" />
        <result column="CONTENT" jdbcType="VARCHAR" property="content" />
    </resultMap>
    <update id="incLikeCount" parameterType="map">
        <!--
          增加点赞数
        -->
        update COMMENT
        set LIKE_COUNT = LIKE_COUNT + #{record.likeCount, jdbcType=INTEGER}
        where id = #{record.id, jdbcType=BIGINT}
    </update>
    <update id="incCommentCount" parameterType="map">
        <!--
          增加评论评论数
        -->
        update COMMENT
        set COMMENT_COUNT = COMMENT_COUNT + #{record.commentCount, jdbcType=INTEGER}
        where id = #{record.id, jdbcType=BIGINT}
    </update>
    </mapper>